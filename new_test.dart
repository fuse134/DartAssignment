import 'dart:io';

void main() {
  print("\n please enter number of student :");
  int no = int.parse(stdin.readLineSync());
  var new_score = List(no);
  for (var no in new_score);
  print("\n Enter score student : #$no");
  double score = double.parse(stdin.readLineSync());
}
